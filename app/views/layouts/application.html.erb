<!DOCTYPE html>
<html>
	<head>
		<title>Chatroom</title>
		<%= csrf_meta_tags %>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>
	<body style="background-color: #222; ">
		<div class="container" style="margin-top: 50px;">
			<%= render "rooms/rooms_menu"%>
			<div class="col-xs-9" style="padding: 0;" >
				<% if user_signed_in? %>
					<ul class="nav nav-tabs" style="margin-left: 10px;"><!-- 'tabs-right' for right tabs -->
						<li><%= link_to "Ver Usuarios",root_path, class: "btn btn-primary"%></li>
						<li><%= link_to "Crear Room",new_room_path, class: "btn btn-success"%></li>
						<li><%= link_to "Cerrar Sesión", destroy_user_session_path, :method => :delete, class: "btn btn-danger" %></li>
					</ul>
				<%end%>
				<div class="col-xs-12 panel" style="padding: 40px;">
					<%= yield %>
				</div>
			</div>
		</div>
	</body>
</html>
